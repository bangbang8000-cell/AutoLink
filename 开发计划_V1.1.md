# AutoLink V1.1 开发计划

## 1. 目标文件结构

```
AutoLink V1.0/
├── main.py                  # 入口：加载配置 → 初始化 → 导出
├── models.py                # 数据类：Connection, NetworkObject
├── topology.py              # 拓扑计算：FatTreeTopology（原 ThreeTierNetwork 重构）
├── designer.py              # 协调层：NetworkDesigner（整合参数网 + 存储网）
├── exporter.py              # Excel 导出 + 格式化
├── network_config.ini       # 配置文件（不变）
├── PRD_V1.1.md              # 本文档
└── 开发计划_V1.1.md          # 本文档
```

---

## 2. 分阶段开发任务

### 阶段 1：修复核心计算 Bug（P0）

> **目标**：使程序在默认配置下输出正确完整的连接表。代码仍在单文件中进行，暂不拆分。

| 任务 ID | 任务 | 涉及代码 | 验证方式 |
|---------|------|----------|----------|
| **T1.1** | 修复 `max_2tier` 公式：`k²/(2p)` → `k²/(4p)` | `topology.py` `FatTreeTopology._calc_max_2tier()` | 运行 N=128, k=64, p=8 → 判定为二层 |
| **T1.2** | 修复三层 `leafs_per_pod`：改为 `groups_per_pod × ports_per_server` | `topology.py` `calculate_hierarchy()` | N=1024 → 生成完整连接，验证服务器覆盖率 100% |
| **T1.3** | 修复三层 Spine 全互联逻辑 | `topology.py` `_connect_leaf_to_spine()` | 验证每个 Leaf 连接了所有 Spine |
| **T1.4** | 修复存储网络独立计算 | `designer.py` `calc_network_hierarchy()` | 存储网上行端口使用率正常（0%~100%） |
| **T1.5** | 添加拓扑合法性自检 | `topology.py` `validate()` | 运行后自动检查端口溢出、覆盖率 |

**阶段 1 验收**：默认配置下服务器 100% 覆盖，无端口溢出，Excel 数据完整。

---

### 阶段 2：代码架构重构（P1）

> **目标**：将代码拆分为清晰的多文件模块，不改变功能行为。

| 任务 ID | 任务 | 说明 |
|---------|------|------|
| **T2.1** | 抽取 `models.py` | 移入 `Connection`、`NetworkObject` 类 |
| **T2.2** | 抽取 `topology.py` | 将 `ThreeTierNetwork` 重构为 `FatTreeTopology`，统一三层和二层计算 |
| **T2.3** | 抽取 `designer.py` | `NetworkDesigner` 保留为协调层，包含 `calc_network_hierarchy`、`create_network_objects`、`generate_connections` |
| **T2.4** | 抽取 `exporter.py` | 移入 `generate_server_view`、`generate_switch_view`、`generate_summary_data`、`apply_excel_formatting`、`export_all_connections` |
| **T2.5** | 创建 `main.py`入口 | `if __name__ == "__main__"` → 调 `NetworkDesigner` → 导出 |
| **T2.6** | 消除 `max_2tier` 重复计算 | 统一由 `topology.py` 提供公式函数 |

**阶段 2 验收**：与原单文件版本行为完全一致（通过阶段 1 的验收标准）。

---

### 阶段 3：健壮性与边界测试（P2）

> **目标**：覆盖边界场景，确保代码在极端参数下不崩溃。

| 任务 ID | 任务 |
|---------|------|
| **T3.1** | 边界参数测试：N=1, N=4096, k=32, p=1 等 |
| **T3.2** | 不合理参数提前报错（如 `ports_per_server > switch_ports`） |
| **T3.3** | 端口满时提供明确的错误信息（而非静默跳过） |

---

## 3. 核心计算公式说明

### 3.1 二层最大容量（2-Tier Max Capacity）

```
max_2tier = k² / (4 × p)
```

- `k`：交换机端口数
- `p`：每服务器网卡数
- 推导：每 Leaf 有 k/2 个下联端口，最多 k/2 个 Spine，全互联收敛比 1:1 时，总 Leaf 数 = k/2，服务器总数 = (k/2 × k/2) / p = k²/(4p)

### 3.2 三层 POD 结构

```
groups_per_pod = servers_per_pod / servers_per_group
leaves_per_pod  = groups_per_pod × ports_per_server
total_leaves    = num_pods × leaves_per_pod
total_spines    = total_leaves          // 1:1 收敛
total_cores     = max(1, total_spines // 2)  // 1:1:1 收敛
```

### 3.3 服务器分组规则

```
servers_per_group = min(servers_per_pod // ports_per_server, switch_ports // 2)
```

---

## 4. 时间规划

| 阶段 | 预计工时 | 依赖 |
|------|----------|------|
| 阶段 1：核心修复 | ~60% | 无 |
| 阶段 2：架构重构 | ~25% | 阶段 1 完成 |
| 阶段 3：健壮性 | ~15% | 阶段 2 完成 |

---

## 5. 里程碑

| 里程碑 | 交付物 |
|--------|--------|
| M1 | 修复后单文件程序，产生完整的 Excel 输出 |
| M2 | 多文件模块化项目，与 M1 结果一致 |
| M3 | 通过所有边界测试的生产就绪版本 |
