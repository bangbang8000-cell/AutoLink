# AutoLink V1.1 产品需求文档 (PRD)

## 1. 产品概述

AutoLink 是一款 AI 智算中心网络拓扑自动设计工具。用户输入服务器数量、网卡数量、交换机端口数等参数后，程序自动计算最优的 Leaf-Spine（或 Leaf-Spine-Core）组网方案，并生成完整的设备连接关系表（Excel）。

V1.0 存在若干计算逻辑缺陷，导致大规模场景下连接数据严重缺失。V1.1 以**修复核心计算 Bug** 为首要目标，同时进行适度的架构优化。

---

## 2. 目标用户与场景

| 用户角色 | 使用场景 |
|----------|----------|
| 网络架构师 | 规划 AI 智算中心参数网/存储网的拓扑方案 |
| 布线工程师 | 获取精确的设备端口对应关系，指导物理布线 |
| 采购人员 | 根据输出结果统计交换机、光模块、线缆数量 |

---

## 3. 功能需求

### 3.1 核心计算修复（P0 - 必须完成）

| ID | 问题描述 | 预期行为 | 影响范围 |
|----|----------|----------|----------|
| **FIX-01** | 三层组网中每 POD 只创建 `ports_per_server` 个 Leaf，但连接逻辑期望 `groups × ports_per_server` 个 Leaf，导致 86% 服务器无连接 | `leafs_per_pod = (servers_per_pod / servers_per_group) × ports_per_server`，确保所有服务器都有完整的 1:1 连接 | `ThreeTierNetwork.calculate_hierarchy()` / `create_network_objects()` |
| **FIX-02** | 二层最大容量公式 `max_2tier = k²/(2×p)` 偏差 2 倍，标准公式应为 `k²/(4×p)` | 使用 `k² / (4×p)` 计算二层最大容量，使 2-tier/3-tier 决策正确 | `ThreeTierNetwork.calculate_hierarchy()` |
| **FIX-03** | 三层 Spine 按 POD 分区连接而非全互联，破坏收敛比 | 每个 Leaf 与所有 Spine 全互联，Core 与所有 Spine 全互联 | `ThreeTierNetwork.generate_connections()` |

### 3.2 存储网络修复（P0 - 必须完成）

| ID | 问题描述 | 预期行为 |
|----|----------|----------|
| **FIX-04** | 存储网络上行端口使用率出现负值（-1900%） | 修复 `servers_per_pod` 与 `leafs_per_pod` 关联计算，使存储网独立且正确地计算 |
| **FIX-05** | 存储网仅创建 2 个 Leaf 无法对接 1024 台服务器 | 根据 `storage_switch_ports` 和 `storage_ports_per_server` 独立计算存储网分组 |

### 3.3 代码质量（P1 - 应该完成）

| ID | 描述 |
|----|------|
| **REF-01** | 将 1393 行单文件拆分为 `models.py`、`topology.py`、`exporter.py`、`main.py` |
| **REF-02** | 消除 `max_2tier` 公式在 `ThreeTierNetwork` 和 `NetworkDesigner` 中的重复计算 |
| **REF-03** | 端口分配策略（下联/上联各 50%）保持不变 |
| **REF-04** | 存储网与参数网的 POD 结构解耦，各自独立计算 |

### 3.4 输入输出兼容性（P1）

- 配置文件格式（INI）保持不变
- Excel 输出格式（4 个 Sheet）保持不变
- CLI 调用方式保持不变

---

## 4. 非功能需求

| 类别 | 要求 |
|------|------|
| **正确性** | 所有服务器必须生成完整的参数网 + 存储网连接 |
| **收敛比** | 三层组网 Leaf:Spine:Core 严格 1:1:1，二层组网 Leaf:Spine 严格 1:1 |
| **端口约束** | 任一台交换机的端口使用数不超过其 `max_ports` |
| **可配置性** | 支持 `ports_per_server ∈ {1,2,4,8}`，`switch_ports ∈ {32,40,64,128,256}` |
| **性能** | 1024 台服务器场景下 30 秒内完成计算 + 导出 |

---

## 5. 验收标准

### 5.1 功能验收（以默认配置 k=64, p=8, N=1024 为例）

| 验收项 | 标准 |
|--------|------|
| 服务器覆盖率 | 1024/1024 = 100% |
| 每服务器参数网连接数 | 8 条 |
| 每服务器存储网连接数 | 1 条 |
| 参数网 Leaf 端口使用率 | ≤ 100%（当前：32/32 服务器 + 32/32 上联 = 100%） |
| 存储网上行端口使用率 | 0% ~ 100%（当前异常：-1900%） |
| Excel 输出 | 4 个 Sheet 均包含完整数据，无空行 |

### 5.2 边界验收

| 场景 | 参数 | 预期结果 |
|------|------|----------|
| 小规模单 POD | N=128, p=8, k=64 | 二层组网，1 组 |
| 中等规模多组 | N=256, p=8, k=64 | 二层或三层边界，需验证 |
| 大规模 | N=1024, p=8, k=64 | 三层组网，4 POD |
| 存储网单独测试 | N=1024, p=1, k=40 | 二/三层正确判定 |

---

## 6. 风险与约束

| 风险 | 缓解措施 |
|------|----------|
| Fat-Tree 拓扑推导复杂，修改后可能引入新的连接错误 | 增加拓扑自检：生成后遍历所有交换机验证端口使用数 |
| 代码拆分后模块间接口不一致 | 拆分前定义清晰的类接口和数据流 |
| 向后兼容性 | 保持 `Connection`、`NetworkObject` 类接口不变 |

---

## 7. 版本信息

| 项目 | 值 |
|------|-----|
| 当前版本 | V1.0 |
| 目标版本 | V1.1 |
| Build 号 | 待定（发布时生成） |
| 文档日期 | 2026-07-26 |
