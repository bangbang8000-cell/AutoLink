# AutoLink V2.0 开发计划

## 总体策略

**分阶段交付，每阶段产出可运行的中间版本。** 借鉴 MagicCommander 的成熟架构，大量复用其脚手架代码（ESLint/Prettier/Vite/Tailwind/Electron配置）。

**UI 对齐 MagicCommander 的核心模式：**
- `App.tsx` → Header + ActivityBar + ResizableAppLayout + StatusBar + ToastContainer
- `ActivityBar` → VS Code 风格左侧图标导航
- `ExplorerPanel` → 项目列表 + 模板中心（双Tab切换）
- `WorkbenchPanel` → 工作台（ScopeCard + ReadinessCard + OutputCard + ActionCard + ResultCard）
- `render.store` → 渲染状态管理（进度/错误/结果/DryRun）

---

## Phase 0: 项目脚手架搭建 (预计 1-2 天)

### 目标
搭建 Electron + React + TypeScript + TailwindCSS 项目骨架，对齐 MagicCommander 的工程规范。

### 任务清单
| # | 任务 | 说明 |
|---|------|------|
| 0.1 | 初始化项目 | `npm create vite@latest` → React + TypeScript |
| 0.2 | 安装依赖 | 照抄 MagicCommander 的 package.json dependencies + devDependencies |
| 0.3 | 配置 Electron 主进程 | `electron/main.ts`、`electron/preload.ts`、`electron/config.ts`（照抄） |
| 0.4 | 配置构建链 | TailwindCSS 3、PostCSS、ESLint、Prettier、tsconfig（照抄） |
| 0.5 | 配置 electron-builder | NSIS安装包，extraResources 挂载 backend/ + workspace/ + template/ |
| 0.6 | 迁移 Python 引擎 | 现有 .py 文件移入 `backend/`，新增 `engine.py` 统一 JSON 接口 |
| 0.7 | Python IPC 桥接 | `electron/services/python.service.ts`：spawn Python 子进程，JSON stdin/stdout 通信 |
| 0.8 | 创建目录结构 | `workspace/`、`template/`、`docs/`、`.github/workflows/` 目录 |
| 0.9 | 内置模板 | 创建 `template/H100-100台/` 和 `template/空项目/` 两个内置模板 |
| 0.10 | GitHub Actions | 创建 `ci.yml`（PR检查）和 `build.yml`（Release构建），对齐 MagicCommander |
| 0.11 | 验证 | `npm run dev:all` 启动 Electron + Vite，确认 Python 子进程可调用 |

### 产出物
- 可启动的 Electron 窗口，Python 引擎可被调用
- workspace/ 和 template/ 目录就绪
- GitHub Actions CI/CD 就绪
- `npm run typecheck` + `npm run lint` 通过

---

## Phase 1: AppShell + 主题 + i18n框架 + 项目管理 + 工作台 (预计 4-6 天)

### 1.1 AppShell 布局框架 (1.5天)

**照抄 MagicCommander 的 App.tsx 布局结构，同时搭建主题和国际化基础设施：**

| # | 任务 | 说明 |
|---|------|------|
| 1.1.1 | `ui.store.ts` | Zustand：活动面板、侧边栏、主题模式(light/dark/system)、语言、底部面板 |
| 1.1.2 | `Header.tsx` | 标题栏：菜单按钮 + 项目名 + 主题切换(ThemePopover) + 语言切换(LanguagePopover) + 窗口控制 |
| 1.1.3 | `ActivityBar.tsx` | 左侧导航：项目、工作台、设计、机柜、拓扑、输出、设置 |
| 1.1.4 | `StatusBar.tsx` | 底部状态栏：项目路径、设计状态、服务器数量、版本号、更新状态 |
| 1.1.5 | `ResizableAppLayout.tsx` | 左侧面板 + 主内容 + 底部面板，可拖拽（照抄 MagicCommander） |
| 1.1.6 | `App.tsx` | 整合上述组件，对齐 MagicCommander 结构 |
| 1.1.7 | **亮暗主题系统** | Tailwind `darkMode: 'class'`，`<html class="dark">` 切换，`Window.matchMedia` 监听系统主题，Zustand persist 持久化，ECharts 主题配色适配 |
| 1.1.8 | **i18n 国际化框架** | i18next + react-i18next 初始化，5语言资源文件骨架（zh-CN/en/ja/ko/zh-TW），`LanguagePopover` 组件，Zustand persist 持久化语言选择 |

### 1.2 项目管理 (1.5天)

**照抄 MagicCommander 的 ExplorerPanel + project.store：**

| # | 任务 | 说明 |
|---|------|------|
| 1.2.1 | `project.store.ts` | Zustand (persist)：项目列表、选中、收藏、最近、模板 |
| 1.2.2 | `ExplorerPanel.tsx` | 双Tab：项目列表 + 模板中心 |
| 1.2.3 | `ProjectListToolbar.tsx` | 工具栏：搜索、排序、新建、保存模板 |
| 1.2.4 | `ProjectListItem.tsx` | 列表项：名称、状态、收藏、右键菜单 |
| 1.2.5 | 项目分组 | 收藏夹、最近使用、全部项目 |
| 1.2.6 | 创建项目 Modal | 项目名输入 + 模板选择 / 空项目 |
| 1.2.7 | 删除项目 | 单个/批量，二次确认 Modal |
| 1.2.8 | 模板中心 | `TemplateCenterPanel.tsx`：模板卡片列表、从模板创建、保存为模板、删除 |
| 1.2.9 | IPC 通道 | `electron/ipc/handlers.ts`：project.list, project.create, project.delete, project.getStructure |

### 1.3 工作台 (1.5天)

**照抄 MagicCommander 的 WorkbenchPanel + render.store：**

| # | 任务 | 说明 |
|---|------|------|
| 1.3.1 | `render.store.ts` | Zustand：渲染状态、进度、错误、结果、输出类型 |
| 1.3.2 | `WorkbenchPanel.tsx` | 主面板，组合子卡片 |
| 1.3.3 | `WorkbenchScopeCard.tsx` | 项目范围选择（当前/批量） |
| 1.3.4 | `WorkbenchReadinessCard.tsx` | 就绪检查（拓扑状态、机柜状态、校验按钮） |
| 1.3.5 | `WorkbenchOutputCard.tsx` | 输出类型勾选（连接关系表、上机表、拓扑图、设备清单） |
| 1.3.6 | `WorkbenchActionCard.tsx` | 渲染按钮（一键渲染、预览、清除） |
| 1.3.7 | `WorkbenchResultCard.tsx` | 渲染结果展示（成功/失败、时间戳、打开文件夹） |
| 1.3.8 | Python IPC 渲染 | `engine.py` 接收渲染指令 → 调用 exporter.py 生成 Excel |
| 1.3.9 | **OutputPanel + 内建预览** | 输出文件树(`OutputFileTree`) + Excel 内建预览(`ExcelPreview`, xlsx库解析) + 图片预览(`ImagePreview`) + Sheet切换 + 搜索筛选 + 打开文件夹 |

### 产出物
- 完整的 AppShell 布局 + 项目管理 + 工作台
- 可创建项目、从模板创建、渲染连接关系表

---

## Phase 2: 拓扑设计 GUI + 配置面板 (预计 2-3 天)

### 目标
把现有 CLI 功能完全 GUI 化。

### 任务清单
| # | 任务 | 说明 |
|---|------|------|
| 2.1 | `design.store.ts` | Zustand：设计参数、摘要、验证结果 |
| 2.2 | `DesignPanel.tsx` | 配置面板 + 设计摘要 + 验证结果 |
| 2.3 | 配置表单 | network_config.ini 的 GUI 表单（下拉选择 + 数字输入 + tooltip） |
| 2.4 | 生成拓扑按钮 | 调用 Python 引擎 → 更新摘要面板 |
| 2.5 | 设计摘要面板 | 展示 Leaf/Spine 数量、端口使用率、收敛比 |
| 2.6 | 拓扑验证展示 | 验证结果（通过/错误列表） |
| 2.7 | full/custom 模式切换 | 实时切换预览配置变化 |

### 产出物
- 完整的拓扑设计 GUI，可替代 CLI 使用

---

## Phase 3: 机柜布局规划 + 拓扑可视化 (预计 5-7 天)

### 3.1 机柜布局规划 (3天)

| # | 任务 | 说明 |
|---|------|------|
| 3.1.1 | `rack.store.ts` | Zustand：机柜列表、设备分配、U位冲突 |
| 3.1.2 | `RackPanel.tsx` | 主面板：机柜列表 + 设备池 + 机柜视图 |
| 3.1.3 | `RackList.tsx` | 机柜列表（增删改，U位使用率进度条） |
| 3.1.4 | `DevicePool.tsx` | 待分配设备池（从拓扑结果提取） |
| 3.1.5 | `RackView.tsx` | 42U机柜正面视图（Canvas/纯CSS 色块） |
| 3.1.6 | 设备分配 | 拖拽设备到U位 / 表单指定U位范围 |
| 3.1.7 | U位冲突检测 | 重叠自动标红提示 |
| 3.1.8 | 设备详情 tooltip | Hover 显示设备名、类型、U位、端口 |
| 3.1.9 | 上机表导出 | 将机柜布局数据导出为 Excel 上机表 |

### 3.2 网络拓扑可视化 (3天)

| # | 任务 | 说明 |
|---|------|------|
| 3.2.1 | `TopologyPanel.tsx` | 主面板：筛选栏 + 拓扑图 + 详情面板 |
| 3.2.2 | `FilterBar.tsx` | 网络类型筛选（参数/存储/OOB/业务） |
| 3.2.3 | `TopologyGraph.tsx` | ECharts Graph 渲染（分层布局） |
| 3.2.4 | 分层布局算法 | Server层 → Leaf层 → Spine层 → Core层，自动计算坐标 |
| 3.2.5 | 节点样式 | 不同设备类型不同颜色/形状/大小 |
| 3.2.6 | `DeviceDetail.tsx` | 点击设备 → 连接列表、端口使用率 |
| 3.2.7 | 连接线高亮 | Hover 设备时高亮相关连接 |
| 3.2.8 | 拓扑导出 | 导出为 PNG（ECharts getDataURL） |

### 产出物
- 完整的机柜U位规划可视化
- 完整的网络拓扑可视化

---

## Phase 4: 国际化补全 + 自动更新 + 打磨 (预计 2-3 天)

### 目标
完善产品体验，对齐 MagicCommander 的品质标准。Phase 1 已搭建主题和 i18n 框架，本阶段重点是补全翻译和集成自动更新。

### 4.1 国际化翻译补全 (1天)

| # | 任务 | 说明 |
|---|------|------|
| 4.1.1 | 简体中文翻译 | 补全所有 namespace（common/project/design/rack/topology/workbench） |
| 4.1.2 | English 翻译 | 完整英文翻译 |
| 4.1.3 | 日本語 翻译 | 完整日文翻译 |
| 4.1.4 | 한국어 翻译 | 完整韩文翻译 |
| 4.1.5 | 繁體中文 翻译 | 完整繁体中文翻译（可批量简→繁转换） |
| 4.1.6 | 翻译验证 | 各语言 UI 走查，确保无截断/溢出 |

### 4.2 自动更新集成 (0.5天)

| # | 任务 | 说明 |
|---|------|------|
| 4.2.1 | `electron/services/update.service.ts` | 封装 electron-updater，autoUpdater 生命周期管理 |
| 4.2.2 | `UpdatePopover.tsx` | Header 更新气泡提示（发现新版本 / 下载进度 / 就绪重启） |
| 4.2.3 | 启动静默检查 | 应用启动后延迟 3 秒自动检查更新 |
| 4.2.4 | electron-builder publish 配置 | GitHub Releases 发布通道配置 |
| 4.2.5 | IPC 通道 | `app:check-update` / `app:download-update` / `app:quit-and-install` |

### 4.3 打磨 (0.5天)

| # | 任务 | 说明 |
|---|------|------|
| 4.3.1 | 快捷键系统 | Ctrl+Shift+E 项目、Ctrl+Shift+W 工作台等（对齐 MagicCommander） |
| 4.3.2 | `SettingsPanel.tsx` | 设置面板（语言、主题、Python路径等） |
| 4.3.3 | 错误处理 | IPC 失败重试、Python 引擎异常 Toast 提示 |
| 4.3.4 | Toast 通知 | 成功/错误/警告 Toast（对齐 MagicCommander） |
| 4.3.5 | 关于对话框 | 版本号、GitHub链接 |

---

## Phase 5: 打包发布 (预计 1 天)

### 任务清单
| # | 任务 | 说明 |
|---|------|------|
| 5.1 | Windows 打包 | `npm run dist:win` → NSIS 安装包 |
| 5.2 | CI/CD | GitHub Actions 自动构建 |
| 5.3 | 发布 Release | GitHub Releases 发布 |

---

## 时间估算

| Phase | 内容 | 预计 |
|-------|------|:--:|
| Phase 0 | 脚手架 | 1-2 天 |
| Phase 1 | AppShell + 主题 + i18n框架 + 项目管理 + 工作台 | 4-6 天 |
| Phase 2 | 拓扑设计 GUI | 2-3 天 |
| Phase 3 | 机柜规划 + 拓扑可视化 | 5-7 天 |
| Phase 4 | i18n翻译补全 + 自动更新 + 打磨 | 2-3 天 |
| Phase 5 | 打包发布 | 1 天 |
| **总计** | | **15-22 天** |

---

## MagicCommander 对照表

| MagicCommander 组件 | AutoLink V2.0 对应 | 复用程度 |
|---------------------|-------------------|---------|
| `App.tsx` (Header+ActivityBar+ResizableLayout+StatusBar) | 同结构 | 照抄 |
| `ui.store.ts` (Zustand persist) | 同结构 | 照抄 |
| `project.store.ts` (Zustand persist) | 同结构，字段调整 | 80% |
| `render.store.ts` | 同结构，字段调整 | 80% |
| `ActivityBar.tsx` (图标导航) | 同结构，图标不同 | 90% |
| `Header.tsx` | 同结构 | 80% |
| `StatusBar.tsx` | 同结构 | 80% |
| `ResizableAppLayout.tsx` | 同结构 | 95% |
| `ExplorerPanel.tsx` (项目列表+模板中心) | 同结构 | 70% |
| `WorkbenchPanel.tsx` (Scope+Readiness+Output+Action+Result) | 同结构，卡片内容不同 | 60% |
| `OutputPanel.tsx` | 同结构 | 70% |
| `SettingsPanel.tsx` | 简化版 | 50% |
| `Toast.tsx` / `Modal.tsx` / `Button.tsx` | 照抄 | 95% |
| `i18n/` (i18next) | 照抄框架，5语言翻译内容不同 | 90% |
| `ThemePopover.tsx` | 照抄，三种模式 (light/dark/system) | 90% |
| `LanguagePopover.tsx` | 照抄，5种语言选项 | 90% |
| `UpdatePopover.tsx` | 照抄，GitHub Releases 更新 | 90% |
| `electron/services/update.service.ts` | 照抄 electron-updater 封装 | 90% |
| `electron/main.ts` + `preload.ts` | 照抄框架 | 80% |

---

## 风险与建议

### 风险
1. **ECharts 拓扑图分层布局**：ECharts Graph 默认是 force-directed 布局，需要自定义分层布局（可参考 ECharts 的 `layout` 和 `categories` 配置）
2. **Python 嵌入式打包**：需确认 Python 3.13 的 embedded 版本与 `openpyxl`、`pandas` 兼容
3. **机柜拖拽交互**：U位精确拖拽需要像素级计算，建议先用表单方式分配，拖拽作为 P2

### 核心建议
1. **Phase 0-1 是重中之重**：跑通整个链路（Electron → Zustand → IPC → Python → JSON），后续只是加功能
2. **照抄 MagicCommander 的脚手架代码**：`electron/main.ts`、`preload.ts`、`config.ts`、`ResizableAppLayout.tsx`、`ui.store.ts` 这些几乎可以直接复用
3. **主题和 i18n 框架在 Phase 1 就搭建好**：Tailwind dark mode 和 i18next 初始化做到 AppShell 中，后续组件开发时同步写 `dark:` 样式和 `t()` 翻译调用，避免最后补
4. **机柜U位可视化用纯 CSS Grid**：42U 的色块用 CSS Grid 布局，简单高效，不需要 Canvas
5. **拓扑图用 ECharts Graph**：分层布局配置 `layout: 'none'` + 手动计算节点坐标，比 D3.js 少很多样板代码