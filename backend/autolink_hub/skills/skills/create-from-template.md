## 技能: 从模板创建项目

当用户要求"创建项目"或"新建项目"时，按以下步骤执行：

1. 如果用户提到具体模板名称，使用 `create_project` 工具并传入 `templateName`（可选）。
2. 如果用户未指定模板，先调用 `list_templates` 查看可用模板，向用户推荐合适的场景模板。
3. 创建完成后使用 `get_project_info` 确认项目结构。
4. 用自然语言向用户汇报项目名、所选模板与下一步建议（如"开始设计网络"）。

注意事项：项目名使用用户给出的名称；未给定时先询问用户。
