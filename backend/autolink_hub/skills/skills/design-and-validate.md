## 技能: 设计并校验

当用户要求"设计网络"、"生成拓扑"或"一键设计"时，按以下步骤执行：

1. 调用 `generate_design` 工具（参数：`projectName`，可选 `configFile`）生成网络设计。
2. 若设计返回校验错误，调用 `validate_design` 检查配置并读取错误明细。
3. 根据错误信息向用户解释原因，并给出修复建议（参数/拓扑相关）。
4. 用自然语言汇报：拓扑规模（设备/链路数量）、收敛比、关键结论。

注意事项：设计为只读类工具（AUTO），可直接执行；涉及导出文件时先询问输出目录。
