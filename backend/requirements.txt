# AutoLink V3.0.0-T0-7: 后端运行时依赖（PyInstaller 打包 + 免 Python 运行）
# 安装: pip install -r backend/requirements.txt
openpyxl>=3.1.0
pandas>=2.0.0
reportlab>=4.0.0
matplotlib>=3.7.0
# V3.1.1-T5-8: AI Hub LLM 客户端（openai 官方库 OpenAI 兼容 + httpx 直连测试）
openai>=1.40.0
httpx>=0.27.0
# M3b: AI Hub 独立进程 HTTP 栈（FastAPI + SSE，对齐 MC ai_hub）
fastapi>=0.110.0
uvicorn>=0.27.0
sse-starlette>=1.6.0
pydantic>=2.5.0
# 5.0.3-503-c: MCP 工具接入（Model Context Protocol，协议层非 Agent 框架；运行期惰性导入）
mcp>=1.2.0
