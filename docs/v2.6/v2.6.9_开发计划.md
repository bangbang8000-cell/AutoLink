# AutoLink v2.6.9 — 缺陷修复开发计划

> 版本:v2.6.9 | 关联 PRD:[v2.6.9_PRD.md](./v2.6.9_PRD.md) | 日期:2026-08-01

## 开发策略

共 3 个问题、6 个任务，每个问题独立修复，可单独测试。

---

## 阶段一: 问题 1 — 拖拽线颜色修复

### T1: 修复 resize handle 暗色类名

**文件**:`src/components/layout/ResizableAppLayout.tsx`

**内容**:
- 第 61 行: `dark:bg-edge-default` → `dark:bg-edge`
- 第 71 行: `dark:bg-edge-default` → `dark:bg-edge`

**根因**: Tailwind `edge: { DEFAULT: '#484F58' }` 的 DEFAULT 键生成 `bg-edge`，不是 `bg-edge-default`。`bg-edge-default` 类不存在，回退到 `bg-gray-200`（白色）。

**验证**: 暗色模式下拖拽线为暗色 `#484F58`，hover 变蓝。

---

## 阶段二: 问题 2 — 更新下载修复

### T2: build.yml 中手动生成 latest.yml

**文件**:`.github/workflows/build.yml`

**内容**:
在 Release 步骤的 "Create GitHub Release" 之前，新增一个步骤 "Generate latest.yml"，为每个平台生成对应的 `latest.yml` 文件。

**latest.yml 格式** (以 Windows 为例):
```yaml
version: 2.6.9
files:
  - url: AutoLink-Setup-2.6.9-win.exe
    sha512: <sha512>
    size: <size>
path: AutoLink-Setup-2.6.9-win.exe
sha512: <sha512>
releaseDate: 2026-08-01T00:00:00.000Z
```

**实现方式**:
```bash
# 在 Release job 中，下载 build artifacts 后，为每个平台生成 latest.yml
# 使用 sha512sum 计算文件 hash，ls -l 获取文件大小
# 然后使用 gh release upload 将 latest.yml 上传到 release
```

**验证**: Release 创建后，访问 `https://github.com/bangbang8000-cell/AutoLink/releases/latest/download/latest.yml` 能返回正确的 YAML 内容。

---

### T3: downloadUpdate 增加 fallback 到手动下载

**文件**:`electron/services/update.service.ts`

**内容**:
`downloadUpdate()` 方法中，当 `updater.downloadUpdate()` 抛出错误时，不直接 reject，而是尝试 `openReleasesPage()` 并返回成功（让用户手动下载）。

**改动**:
```typescript
async downloadUpdate(): Promise<void> {
  const updater = await getAutoUpdater()
  if (!updater) {
    throw new Error('electron-updater not available')
  }

  // ... 清理监听器 ...

  return new Promise<void>((resolve, reject) => {
    updater.on('download-progress', ...)
    updater.on('update-downloaded', () => { ... resolve() })
    updater.on('error', (err: Error) => {
      // v2.6.9: 下载失败时回退到打开 Releases 页面
      console.warn('[UpdateService] Download failed, opening releases page:', err.message)
      this.mainWindow?.webContents.send('update:error', err.message)
      this.openReleasesPage()
      resolve() // 不算失败，用户可以去手动下载
    })

    updater.downloadUpdate().catch((err) => {
      console.warn('[UpdateService] downloadUpdate() threw, opening releases page:', err.message)
      this.mainWindow?.webContents.send('update:error', err.message)
      this.openReleasesPage()
      resolve()
    })
  })
}
```

**验证**: 模拟下载失败场景，确认自动打开 GitHub Releases 页面。

---

### T4: UpdatePopover 下载失败提示优化

**文件**:`src/components/layout/UpdatePopover.tsx`

**内容**:
当接收到 `update:error` 事件后，状态切换到 `error` 时，显示"下载失败，已为您打开下载页面"的提示，并保留"手动下载"按钮。

**改动**:
- 在 `error` 状态下，文案改为"下载失败，已跳转到浏览器下载页面"
- 确保"手动下载"按钮始终可用

**验证**: 模拟下载失败，UI 提示正确。

---

## 阶段三: 问题 3 — 输出文件显示修复

### T5: 统一输出文件路径格式

**涉及文件**:
- `electron/ipc/handlers.ts`（`listOutputBatches`）
- `src/components/layout/FileExplorer.tsx`（`handleBatchFileClick`、`OutputSection.handleFileClick`、`buildBatchFileContextMenu`）

**内容**:

**后端**:
`listOutputBatches` 返回的 `file.path` 从 `"${projectName}/output/${batch}/${file}"` 改为 `"output/${batch}/${file}"`（相对路径，不含项目名）。

**前端**:
1. `handleBatchFileClick` 的 `openTab` state 增加 `projectName: _projectName`。
2. `OutputSection.handleFileClick` 的 `openTab` state 增加 `projectName`（需要将 `projectName` 传入 `handleFileClick`）。
3. `OutputSection` 中所有 `handleFileClick` 调用点传入 `projectName` 参数。
4. `buildBatchFileContextMenu` 和 `buildFileContextMenu` 中 `openFile` action 也传入 `projectName`。

**WorkspaceView 路径拼接逻辑**:
现有逻辑 ([WorkspaceView.tsx#L86-L99](file:///d:/MyCoding/AutoLink/AutoLink_v2.0/src/components/workspace/WorkspaceView.tsx#L86-L99)) 已正确处理：当 `projectName` 存在且 `!isTemplate` 时，拼接 `${projectName}/${filePath}`。所以 `filePath` 应为纯相对路径，由 `WorkspaceView` 拼接项目名前缀。

**验证**:
- 手动测试：渲染项目后，展开输出文件 Section，能看到批次
- 点击批次文件能正常打开预览
- 项目下的"输出文件"智能分组也能正常显示

---

### T6: 回归测试

**内容**:
- `tsc --noEmit` — 0 errors
- `eslint` — 0 errors（警告不增加）
- `vitest run` — 全通过
- 手动验证三个问题的修复

---

## 任务依赖关系

```
T1 (拖拽线) ──→ 独立，无依赖
T2 (build.yml) ──→ 独立，无依赖
T3 (downloadUpdate fallback) ──→ 独立，无依赖
T4 (UpdatePopover 优化) ──→ 依赖 T3
T5 (输出文件路径) ──→ 独立，无依赖
T6 (回归测试) ──→ 依赖全部
```

**建议执行顺序**: T1 → T5 → T3 → T4 → T2 → T6

---

## 实施状态

> 更新时间:2026-08-01

| 任务 | 状态 | 说明 |
|------|------|------|
| T1 拖拽线颜色修复 | ⏳ 待实施 | `dark:bg-edge-default` → `dark:bg-edge` |
| T2 build.yml 生成 latest.yml | ⏳ 待实施 | Release 步骤中手动生成并上传 |
| T3 downloadUpdate fallback | ⏳ 待实施 | 下载失败时打开 Releases 页面 |
| T4 UpdatePopover 提示优化 | ⏳ 待实施 | 错误文案优化 |
| T5 输出文件路径统一 | ⏳ 待实施 | 后端 path 格式 + 前端传 projectName |
| T6 回归测试 | ⏳ 待实施 | — |